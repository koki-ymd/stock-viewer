# =========================
# 実行環境
# =========================
# "local" / "prod" など。未設定なら local 扱いにしておく。
APP_ENV=local

# ローカル開発時のフロントエンド URL
# 例）http://localhost:5173
# 本番（Cloud Run モノリス）では基本的に同一オリジンなので未設定でOK
FRONTEND_ORIGIN=http://localhost:5173

# ===========================
# JWT 認証設定
# ===========================

# JWT を署名するための秘密鍵です。
# 文字列なら何でも動きますが、実運用では十分な長さのランダム文字列を設定してください。
JWT_SECRET_KEY=your-secret-key-here

# JWT の署名アルゴリズム。
# HS256（HMAC-SHA256）が一般的です。
JWT_ALGORITHM=HS256

# アクセストークンの有効期限（分）。
# 例：60 = 1時間、1440 = 1日
JWT_EXPIRE_MINUTES=60

